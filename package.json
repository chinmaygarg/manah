{
  "private": true,
  "name": "manah-monorepo",
  "scripts": {
    "build": "cd website && npm run build",
    "dev": "cd website && npm run dev"
  },
  "dependencies": {
    "next": "^16.1.4"
  }
}
